EXAMPLE:

  I wish to make my room non-follow but I wish for the players to be able to teleport out.

  PREVIOUS CODE:

	  reset(arg)
	  {
            /*
            */
	  }
	  realm() { return "NT"; }
	  okay_follow() { return 1; }

  NEW CODE:

      reset(arg)
      {
        /*
         */
        add_property("no_follow");
      }
