inherit "obj/armor";

reset(arg) {
if(!arg) {
   set_id("headband");
   set_name("dark leather headband");
   set_short("Dark leather headband");
   set_value(50);
   set_weight(1);
   set_ac(1);
   set_alias("helmet");
   set_long("A thick, weathered, leather headband with strange symbols\n"+
            "across the top.  It gives slight protection to the head.\n");
   set_type("helmet");
   set_info("A headband from a dead Scoti Raider.  It offers average protection to the head.\n");
   }
}
