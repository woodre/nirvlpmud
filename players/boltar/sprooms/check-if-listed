check-if-listed(arg){
  string me,str;
  sscanf(arg, "%s %s",me,str);
  while(i < CAP +1) {
    if(parents[i] == me && names[i] == str) {
      return 99;
    }
    if(parent_too[i] == me && names[i] == str) {
      return 99;
    }
    i += 1;
  }
  return 0;
}

