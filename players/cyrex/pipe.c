inherit "obj/weapon";
object pipe;
reset(arg){
::reset(arg);
set_name("pipe");
set_alias("pipe");
set_short("An old oak pipe");
set_long("A oak pipe that seems to have amazing properties. It looks like it can be wielded and puffed on.\n");
set_class(10);
set_weight(2);
set_value(250);
}
