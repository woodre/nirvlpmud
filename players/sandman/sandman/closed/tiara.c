inherit"obj/armor";
reset(arg) {
::reset(arg);
if(arg) return;
set_name("glittering tiara");
set_alias("tiara");
set_short("Glitering Tiara");
set_long(
"A glittering crown made of pearls and crystals that shimmer with a\n"+
"cathedral length veil.\n");
set_weight(1);
set_value(0);
set_ac(1);
set_type("helmet");
set_arm_light(1);
}
