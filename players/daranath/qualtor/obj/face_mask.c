inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("mask");
   set_short("A black face mask");
   set_alias("face mask");
   set_long("The black mask entirly covers one's face in black cloth.\n");
   set_type("helmet");
   set_ac(1);
   set_weight(1);
   set_value(100);
}
