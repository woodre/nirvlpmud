inherit "obj/treasure";
reset(arg) {
  if(arg) return;

    set_id("goblet");
    set_alias("gob");
    set_short("A crystalline goblet");
    set_long("A goblet made from pure crystal.\n");
    set_weight(1);
    set_value(850);
}
