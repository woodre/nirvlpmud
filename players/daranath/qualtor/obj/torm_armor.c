inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("platemail");
   set_short("Black spiked platemail");
   set_alias("plate");
   set_long("Blackened platemail with wicked spikes set at the shoulders,\n"+
            "elbows and every other place the smith could fit one.\n");
   set_type("armor");
   set_ac(4);
   set_weight(3);
   set_value(4750);
}
