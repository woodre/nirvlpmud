inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("hairpins");
   set_short("A set of strong hairpins");
   set_alias("pins");
   set_long("A pair of silver hairpins used for both substance and style.\n");
   set_type("helmet");
   set_ac(1);
   set_weight(1);
   set_value(400);
}
