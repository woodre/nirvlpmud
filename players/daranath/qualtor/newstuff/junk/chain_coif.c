inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("chain coif");
   set_short("Chainmail coif");
   set_alias("coif");
set_long("A chain coif that covers your head without a helm during a fight.\n");
   set_type("helmet");
   set_ac(1);
   set_weight(1);
   set_value(50);
}
