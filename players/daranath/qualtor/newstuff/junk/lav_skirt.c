inherit "obj/armor";
reset(arg){
   ::reset(arg);

   set_name("lavender skirt");
   set_short("A soft lavender skirt");
   set_alias("skirt");
   set_long("A soft, flowing skirt. Not only accentuates the legs, but offers\n"+
            "minimal protection in a fight.\n");
   set_type("misc");
   set_ac(1);
   set_weight(1);
   set_value(200);
}
