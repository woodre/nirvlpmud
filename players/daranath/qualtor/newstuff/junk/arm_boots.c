inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("armored boots");
   set_short("Armored boots");
   set_alias("boots");
set_long("A pair of armored boots, made for protection during combat.\n");
   set_type("boots");
   set_ac(1);
   set_weight(2);
   set_value(200);
}
