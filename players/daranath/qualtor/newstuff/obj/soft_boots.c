inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("soft boots");
   set_short("Soft leather boots");
   set_alias("boots");
   set_long("Soft leather boots, perfect for running through the woods.\n");
   set_type("boots");
   set_ac(1);
   set_weight(1);
   set_value(175);
}
