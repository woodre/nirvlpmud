inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("uniform");
   set_short("A short white nurse's uniform");
   set_alias("dress");
   set_long("The tight white uniform of a nurse clings to the body of whoever\n"+
            "should wear it. Its top button is missing, exposing a healthy amount\n"+
            "of cleavage, while the skirt is so short that bending over would\n"+
            "be illegal in most states.\n");
   set_type("armor");
   set_ac(2);
   set_weight(1);
   set_value(69);
}
