inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("heels");
   set_short("White high heeled shoes");
   set_alias("shoes");
   set_long("A pair of sexy pumps, accenting the curves of the\n"+
            "woman wearing them.\n");
   set_type("boots");
   set_ac(1);
   set_weight(1);
   set_value(69);
}
