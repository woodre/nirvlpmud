inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("stockings");
   set_short("White thigh high stockings");
   set_alias("stocking");
   set_long("A pair of white stocking, with a line running along the back\n"+
            "of the legs. They stop just short of the nurse's uniform.\n");
   set_type("misc");
   set_ac(1);
   set_weight(1);
   set_value(69);
}
