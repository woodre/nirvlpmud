inherit "obj/armor";
reset(arg){
   ::reset(arg);

   set_name("nurse hat");
   set_short("A small white hat with a red cross");
   set_alias("hat");
   set_long("A small white hat commonly worn by nurses to both keep thier\n"+
            "hair away from the patient and increase their sex appeal, the\n"+
            "hat has a small red cross on the front of it.\n");
   set_type("helmet");
   set_ac(1);
   set_weight(1);
   set_value(69);
}
