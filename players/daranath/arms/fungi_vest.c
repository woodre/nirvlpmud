inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("sorceror vest");
   set_short("Fungus sorceror vest");
   set_alias("vest");
  set_long("The decorated vest from the Myconid Sorceror, it is red with\n"+
           "many items sewn into it. It does not offer much protection.\n");
   set_type("armor");
   set_ac(1);
   set_weight(1);
   set_value(50);
}
