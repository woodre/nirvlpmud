inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("studded leather");
   set_short("A suit of studded leather");
   set_alias("leather");
   set_long("A suit of studded leather. Gives some protection in a fight.\n");
   set_type("armor");
   set_ac(3);
   set_weight(2);
   set_value(100);
}
