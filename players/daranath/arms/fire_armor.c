inherit "obj/armor";

reset(arg){
   ::reset(arg);

   set_name("giantplate");
   set_short("A suit of fire red platemail");
   set_alias("platemail");
   set_long("A suit of giant sized armor taken from a recent kill.\n");
   set_type("armor");
   set_ac(5);
   set_weight(7);
   set_value(5500);
   set_save_flag(0);
}
