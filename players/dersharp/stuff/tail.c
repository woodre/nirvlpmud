inherit "obj/treasure";

reset(arg) {
    set_id("tail");
    set_short("A bat's tail");
    set_long("	Maybe you could sell this since it has some value.\n");
    set_weight(1);
    set_value(30);
}
