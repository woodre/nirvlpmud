id(str) {return str == "hut" ;}
short(str) {return "A Filthy Old Hut";}
long() {write("A small half-dwelling that looks and smells awful.\n");
       write("There seems to be an entrance of sorts to the west...\n");
       write("That is...if you REALLY want to go in....\n"); }
get() {return 0;}
drop() {return 1;}
