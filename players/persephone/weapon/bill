inherit "obj/weapon.c";
reset(arg) {
    ::reset();
    if (arg) return;
    set_name("club");
    set_alias("club");
    set_short("A Billyclub");
    set_long("An extremely effective looking billyclub\n");
    set_class(18);
    set_weight(1);
    set_value(1000);
    set_hit_func(this_object());
}
