inherit "obj/armor";
reset(arg){
   ::reset(arg);
    set_short("An old Breastplate");
   set_arm_light(0);
set_long(
"An old Breastplate that has rusted with unuse, However it seems it\n"+
"still might provide soom good protection.\n");
   set_ac(3);
   set_weight(3);
   set_value(80);
   set_alias("plate");
   set_name("kings_crown");
   set_type("armor");
}
