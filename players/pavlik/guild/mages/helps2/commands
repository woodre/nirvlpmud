-=[ Commands ]=-

The 'commands' command will display the additional commands for the
guild (additional being non-spells).  'commands basic' will list the
commands avaliable to all Mages (level 0 commands).  Otherwise only the
additional commands avaliable will be displayed.

