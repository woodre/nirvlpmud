inherit "obj/armor";
reset(arg){
   ::reset(arg);
   set_name("midnight cloak");
   set_short("A cloak of midnight blue");
   set_alias("cloak");
   set_long("A full body cloak of midnight blue.\n"+
       "It has silver trim, and is very soft.\n");
   set_type("misc");
   set_ac(1);
   set_weight(1);
   set_value(50);
}
