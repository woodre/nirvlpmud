inherit "obj/treasure";
reset(arg)
{   if (arg) return;
   set_id("paintball clip");
   set_alias("clip");
   set_short("A clip of paintball ammo");
   set_long("A clip full of paintball ammo.\n");
   set_weight(0);
   set_value(10);
}
