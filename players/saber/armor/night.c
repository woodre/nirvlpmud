inherit "obj/armor";
reset(arg){
   ::reset(arg);
   set_name("helmet of night");
   set_short("Helmet of Night");
   set_alias("helmet");
   set_long("A huge steel helmet which seems to attract shadows.\n");
   set_type("helmet");
   set_ac(1);
   set_weight(1);
   set_value(1000);
   set_light(-2);
}
