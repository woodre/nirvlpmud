inherit"obj/treasure";
reset(arg) {
if(arg) return;
set_id("sign");
set_alias("sign");
set_short("a sign... take a look at it");
set_long(
"this is a new realm and be aware that much is not yet complete....\n"
+ " no rooms to the north have been finished yet.. only east and west.\n"
+ "have fun... and tell me if you find a problem? or suggestions.\n");
set_value(0);
set_weight(20);
}
