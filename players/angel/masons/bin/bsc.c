#include "defs.h"

status main(string str, int glevel) {
   
write("This command has been disabled as it is now part of Nirvana mudlib.\n-Earwax\n");
return 1;
/*
   if(battle == 0) {
       write("Battlescore is turned on.\n");
       battle = 1;
       set_heart_beat(1);
       return 1;
     } else
     battle = 0;
     write("Battlescore is turned off.\n");
     return 1;
*/
}
