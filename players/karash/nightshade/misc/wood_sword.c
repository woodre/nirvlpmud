inherit "obj/treasure";
reset(arg) {
if (arg) return;
set_alias("sword");
set_short("Wooden Sword");
set_long
("The fighters use these swords to practice with. It would not make a good weapon.\n");
set_weight(1);
set_value(500);
}
