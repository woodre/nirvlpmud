inherit "obj/armor";
reset(arg){
   ::reset(arg);
   set_short("Driver's Hat");
   set_arm_light(0);
set_long(
"This is a Pizza Hut Driver's hat, it has many fancy pins on it\n");
   set_ac(1);
   set_weight(1);
   set_value(100);
   set_alias("driver hat");
   set_name("hat");
   set_type("helmet");
}
