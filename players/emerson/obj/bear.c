inherit "obj/treasure";

reset(arg) {
 if(arg) return;
     set_short("A *cute* Teddy Bear");
    set_alias("bear");
set_long("This is the cutest teddy bear you have every seen, someone must think you're special!\n");
   set_weight(1);
    set_value(0);
}

