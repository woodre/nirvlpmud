inherit "obj/treasure";

reset(arg) {
 if(arg) return;
    set_short("A long stem red rose");
    set_alias("rose");
set_long("This is the prettiest rose you have every seen, someone must think you're special!\n");
   set_weight(0);
    set_value(0);
}

