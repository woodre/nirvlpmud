inherit "obj/armor";
reset(arg) {
   if (arg) return;
   set_name("A very long rope");
   set_short("A very long rope");
   set_alias("rope");
   set_weight(2);
   set_value(100);
   set_long("A very long rope with knots in it for climbing purposes.\n");
}
