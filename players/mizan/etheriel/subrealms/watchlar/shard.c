#include "std.h"
id(str) { return str == "shard" || str == "zmxncbv"; }
short() { return "A shard of crystal glass"; }
long() {
write("This is a shard of glass. It looks like it belongs to a mirror,\n"+
"and needs to be placed into it.\n");
}
get() { return 1; }
query_weight() { return 1; }
query_value() { return 10; }

