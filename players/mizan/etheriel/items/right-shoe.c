inherit"obj/treasure";
reset(arg) {
if(arg) return;
set_id("shoe");
set_alias("shoe");
set_short("A right shoe");
set_long(
"This is a really beat up shoe. You can tell it belongs\n"
+ "on the right foot, however.\n");
set_value(120);
set_weight(1);
}
