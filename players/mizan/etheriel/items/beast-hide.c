inherit "obj/armor";
reset(arg) {
::reset(arg);
	set_short("A soft wooly hide");
	set_long("This is the soft, wooly hide of an Perfectly Normal Beast. It looks\n"+
	"comfy, and offers good protection, but its a bit itchy actually.\n");
	set_ac(2);
	set_value(500);
	set_alias("hide");
	set_name("hide");
	set_type("armor");
  set_weight(1);
	}
