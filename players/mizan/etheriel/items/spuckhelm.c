inherit "obj/armor";

reset(arg) {
::reset(arg);
	set_short("Spuckhelm");
	set_long("This is a soft, and extremely comfortable helmet. When you squish\n"+
	"it with your fingers, it makes odd noises, and it seems to stick to your head.\n");
	set_ac(1);
	set_value(900);
	set_weight(1);
	set_alias("helm");
	set_name("spuckhelm");
	set_type("helmet");
	}
