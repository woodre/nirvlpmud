inherit "obj/armor";
reset(arg) {
::reset(arg);
	set_short("Cream platemail");
	set_long(
	"  This is what seems to be a regular piece of platemail, but it has a strange\n"+
	"  cream-colored glow to it, and it seems lighter than you would expect.\n");
	set_ac(3);
	set_value(2000);
	set_alias("platemail");
	set_name("platemail");
	set_type("armor");
  set_weight(3);
}
