#include "std.h"
id(str) { return str == "corsair" || str=="ship" || str == "spaceship"; }
short() { return "A darkwing corsair"; }
long() {
write("  You see what looks to be some kind of a spaceship in the distance.\n"+
"  It does not look like there are any obvious exits into the ship.\n");
	}
