inherit "/obj/treasure.c";

reset (arg) {
	set_id("fangs");
	set_short("A pair of fangs");
	set_long(
      "  Filled with the deadly poison of the snake, the fangs\n"+
      "could be sold for a good price.\n"); 
	set_weight(1);
	set_value(400);
}

