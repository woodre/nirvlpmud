inherit "obj/treasure";
reset(arg) {
if (arg) return;
set_short("Onyx");
set_long
("A rather large onyx gem Hephastus has plundered.\n");
set_weight(1);
set_alias("gem");
set_value(500);
}

id(str) { return str == "onyx" || str == "evil_onyx"; }
