inherit "obj/treasure";
reset(arg) {
if (arg) return;
set_short("Cleric's Symbol");
set_long
("This is the clerics symbol of power. He cannot be without it.\n");
set_weight(1);
set_alias("symbol");
set_value(500);
}
