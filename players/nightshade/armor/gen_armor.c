inherit "obj/armor";
reset(arg){
   ::reset(arg);
   set_short("Generic Armor");
set_long(
"Yes folks, it is generic but it does the job does it not?\n");
   set_ac(3);
   set_weight(4);
   set_value(1000);
   set_alias("armor");
   set_name("armor");
   set_type("armor");
}
