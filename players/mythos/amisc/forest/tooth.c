inherit "obj/treasure";
reset(arg) {
  if(arg) return;
    set_id("tooth");
    set_alias("drake tooth");
    set_short("A Drake's tooth");
    set_long("A sharp reptilian tooth.\n");
    set_weight(1);
    set_value(1500);
}
