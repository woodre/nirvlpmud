inherit "obj/treasure";
reset(arg) {
  if(arg) return;
    set_id("key");
    set_alias("key_myrax");
    set_short("Ebon Key");
    set_long("The Key to Myrax- the key that shall open the circle.\n");
    set_weight(1);
    set_value(5000);
}
