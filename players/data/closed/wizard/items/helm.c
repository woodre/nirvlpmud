#include "/players/maledicta/ansi.h"
inherit "obj/armor";

reset(arg) {
set_name("helm");
set_short("helm");
set_long(
  "  This is a small steel helm.  It is used for decent protection\n"+
  "during battle.\n");
set_ac(1);
set_type("helmet");
set_weight(1);
set_value(50);
}

	
