inherit "obj/armor";
reset(arg) {
  set_name("hat");
  set_short("A chef's hat");
  set_alias("helmet");
  set_long(
    "\nA very tall, well starched, white chef's hat. It has\n"+
    "a french look and feel to it, but you know that this hat\n"+
    "actually belonged to the Soup Nazi.\n\n");
  set_ac(1);
  set_type("helmet");
  set_weight(1);
  set_value(100);
  set_save_flag();
}

