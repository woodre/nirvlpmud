#include "../defs.h"

/* A simple inheritable that just defines all the settable parameters 
 * and returns them in a single array, to even further lower the number
 * of function calls.  Nothing big, mebbe like 12 lines total.
*/
inherit CMDBASE;
