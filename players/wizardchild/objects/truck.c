short() {
        return "A truck";
}
long() {
        write("A full sized semi-truck, headed for ACME plaza. It appears\n");
        write("that the driver is staring at the snakes in the road.\n");
        return 1;
}
 
id(str) { return str == "truck"; }
