short() {
  return "A pool of water";
}
id(str) { 
  return str == "pool" || str == "water" || str == "pool of water"; 
}
long()  {
  write("A strange pool of water, with a glossy yet unappealing surface. One look tells\n");
  write("you that its water would not be fit for drinking.\n");
}
