short() { return "A muddy stream"; }
id(str) { return str == "stream" || str == "muddy stream"; }
long()  {
  write("A muddy stream, flowing through the path. It is not more than a foot wide, and\n");
  write("hinders your passage in no way.\n");
}
