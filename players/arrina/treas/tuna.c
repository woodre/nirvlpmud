inherit "obj/treasure";
 reset(arg)  {
  if(arg) return;
    set_id("tuna");
   set_short("A Tuna");
   set_long("This appears to be a normal tuna.\n"+
     "It has no value, it has no purpose.....enjoy.\n");
   set_weight(0);
   set_value(0);
}
