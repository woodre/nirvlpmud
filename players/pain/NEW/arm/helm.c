inherit "obj/armor" ;

reset(arg)
{
  ::reset(arg) ;
    set_short("A horned helm");
    set_long("A large horned helm made by orcs.\n");
     set_ac(1);
     set_weight(2);
	set_value(500);
	set_alias("helm");
	set_name("helm");
	set_type("helmet");
}
