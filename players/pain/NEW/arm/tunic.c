inherit "obj/armor" ;

reset(arg)
{
  ::reset(arg) ;
    set_short("A handsome blue tunic");
    set_long("A handsome blue tunic with the crest of Bigelow on it.\n");
     set_ac(1);
     set_weight(1);
	set_value(300);
	set_alias("tunic");
	set_name("tunic");
	set_type("chest");
  }
