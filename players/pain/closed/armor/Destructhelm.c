inherit "obj/armor" ;

reset(arg)
{
  ::reset(arg) ;
    set_short("The Helm of Destruction");
   set_long("This is the helm of the god Destruction, it seems to glow from an inward spirit\n");
     set_ac(2);
     set_weight(1);
	set_value(100000000000);
	set_alias("helm");
	set_name("helm");
	set_type("helmet");
  }
