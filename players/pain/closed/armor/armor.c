inherit "obj/armor" ;

reset(arg)
{
  ::reset(arg) ;
    set_short("An Example shield");
    set_long("A truly well coded example shield.\n");
     set_ac(1);
     set_weight(1);
	set_value(250);
	set_alias("shield");
	set_name("shield");
	set_type("shield");
  }
