#include <ansi.h>

string short()
{
 return "A "+BLINK+HIM+"neon sign"+NORM+NORM;
}

status id(string str)
{
 return (str=="neon sign" || str == "sign");
}

int get() { return 0; }

void long()
{
 write(BLINK + HIM + "\
 DEPOSITORY OPEN NORTH!\n\n"+NORM+NORM);
}

